// Define all of your configuration variables here in one place
// Examples: database paths, mail servers (smtp settings)
module.exports = {
    // db: 'mongodb://localhost/bobsMongo',
    // dbOptions: { },
}
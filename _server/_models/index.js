function load () {
   // This function loads in all of your models (database either mongo or mysql)
   // example: require('./user.model')
}

// module.exports functions, so that you can require them in other programs.
module.exports = { load };